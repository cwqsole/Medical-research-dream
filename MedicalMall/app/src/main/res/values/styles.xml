<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="LoadingTheme" parent="Theme.AppCompat.Dialog">
        <!--dialog背景设置为透明-->
        <item name="android:windowBackground">@color/transparent</item>
        <!--false:去掉背景半透明效果,如果是true,就会有半透明效果 -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>


    <!--底部导航RadioGroup-->
    <style name="main_bottom_tab_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:drawablePadding">6dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">3dp</item>
        <item name="android:textColor">@color/rb_color</item>
        <item name="android:textSize">11sp</item>
    </style>


    <style name="MyRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingbar</item>
        <item name="android:maxHeight">10dp</item>
        <item name="android:minHeight">13dp</item>
    </style>

    <style name="RememberPswdCheckboxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/checkbox_style1</item>
    </style>

</resources>
